# Caribbean Coral Reef Species Classification — Python dependencies
# Tested with Python 3.10, CUDA 12.x

# ── Core deep learning ────────────────────────────────────────────────
torch>=2.5.1
torchvision>=0.20.1
timm>=1.0.11

# ── Segmentation models ───────────────────────────────────────────────
# SAM 2 is installed separately from source — see README step 4
# MobileSAM and CoralSCOP have their own install instructions
segment-anything>=1.0          # required for MobileSAM & CoralSCOP loaders

# ── Image processing ──────────────────────────────────────────────────
opencv-python>=4.8.0
pillow>=10.0.0
numpy>=1.24.4
scikit-image>=0.21.0

# ── Data handling ─────────────────────────────────────────────────────
pandas>=2.0.0
pyyaml>=6.0

# ── Metrics & evaluation ──────────────────────────────────────────────
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Augmentation ──────────────────────────────────────────────────────
albumentations>=1.3.1

# ── Progress bars & utilities ─────────────────────────────────────────
tqdm>=4.66.0

# ── Configuration ─────────────────────────────────────────────────────
hydra-core>=1.3.2              # required by SAM 2
omegaconf>=2.3.0

# ── Optional but recommended ──────────────────────────────────────────
# tensorboard>=2.14.0          # uncomment for training visualisation
# jupyterlab>=4.0.0            # uncomment for notebook analysis

# ── Label tool (Tkinter GUI) ──────────────────────────────────────────
# tkinter is part of the Python standard library — no pip install needed
# On Ubuntu, ensure python3-tk is installed:
#   sudo apt install python3-tk
